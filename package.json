{
  "name": "xiuxian-2d-action",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc --outDir .test-out --rootDir src --module es2020 --target es2020 --moduleResolution bundler --skipLibCheck --lib ES2020,DOM src/save/SaveV1.ts && node --test tests/save-v1.test.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "6.3.5"
  }
}
